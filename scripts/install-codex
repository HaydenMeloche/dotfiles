#!/usr/bin/env zsh
set -euo pipefail

if command -v codex >/dev/null 2>&1; then
  print "Codex is already installed: $(codex --version)"
  exit 0
fi

# Official standalone installer for macOS and Linux.
curl -fsSL https://chatgpt.com/codex/install.sh | sh
